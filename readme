uv run python test_mini.py
可以看到decompose的system prompt 和 user prompt，以及 DecompositionPlan（后续分解subagent的执行逻辑）

/Users/weichy/code/Galaxy/log中有json版本的DecompositionPlan

我先搭建后面基于这个DecompositionPlan的agent逻辑，可飞、庄禹帮我检查一下这个prompt是否合适。
以及结构化的prompt是否合适？
